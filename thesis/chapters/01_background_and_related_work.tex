\chapter{Background and Related Work}
\label{sec:background}

The increasing adoption of computational experiments and simulation workflows in scientific research has resulted in large volumes of structured data describing parameters, metrics, and results. Ensuring that such data can be validated, exchanged, and semantically interpreted requires standardized mechanisms for describing structure and meaning. This chapter introduces three key technologies that enable this process: JSON for structured data representation, JSON Schema for structural validation, and semantic mappings using the RDF Mapping Language (RML) to produce semantically enriched JSON-LD representations. The discussion is illustrated using a running example from a simulation experiment where the element size parameter influences a computed stress metric.

\section{JSON as a Data Representation Format}
\label{sec:background:json}

JavaScript Object Notation (JSON) is a lightweight data-interchange format widely used for representing structured data in web services, scientific workflows, and configuration systems. JSON encodes data as attribute-value pairs and ordered lists, making it human-readable and easy to parse by machines. Due to its simplicity and broad ecosystem support, JSON has become a de facto standard for structured data exchange across many domains \cite{json_standard}.

In computational science workflows, JSON files often capture experiment parameters and resulting metrics. The following example illustrates a minimal JSON document describing the input parameter \texttt{element-size} and the resulting metric \texttt{max\_von\_mises\_stress\_nodes}.

\begin{lstlisting}[language=json, caption={Example JSON representation of simulation input parameters and results}, label={lst:background_json_example}]
{
    "parameters": {
        "element-size": {
            "value": 0.025,
            "unit": "m"
        }
    },
    "metrics": {
        "max_von_mises_stress_nodes": 299791507.5586333
    }
}
\end{lstlisting}

While JSON provides a convenient serialization format, it does not inherently enforce structural constraints. Therefore, additional mechanisms are required to validate that JSON documents conform to an expected structure.

\section{JSON Schema for Data Validation}
\label{sec:background:json_jsonschema}

JSON Schema provides a standardized mechanism for describing the expected structure and constraints of JSON documents. A JSON Schema defines properties, data types, required attributes, and validation rules for JSON instances. This allows automated validation of data before further processing or transformation. The JSON Schema specification defines a vocabulary for describing JSON documents and their validation rules \cite{wright2020jsonschema}. 

Listing \ref{lst:background_json_schema} shows a JSON Schema corresponding to the example JSON document in Listing \ref{lst:background_json_example}. The schema ensures that the JSON document contains the expected parameter and metric fields and enforces the correct numeric data types.

\begin{lstlisting}[language=json, caption={JSON Schema validating the simulation result JSON document}, label={lst:background_json_schema}]
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "parameters": {
      "type": "object",
      "properties": {
        "element-size": {
          "type": "object",
          "properties": {
            "value": {
              "type": "number"
            },
            "unit": {
              "type": "string"
            }
          },
          "required": ["value", "unit"]
        }
      },
      "required": ["element-size"]
    },
    "metrics": {
      "type": "object",
      "properties": {
        "max_von_mises_stress_nodes": {
          "type": "number"
        }
      },
      "required": ["max_von_mises_stress_nodes"]
    }
  },
  "required": ["parameters", "metrics"]
}
\end{lstlisting}

Using JSON Schema enables consistent validation of experiment data and ensures that all required values are present before transformation into other representations.

However, JSON Schema focuses primarily on structural validation and does not provide semantic meaning or interoperability with knowledge graph technologies. To address this limitation, semantic mapping techniques can be applied.

\section{Ontology: Modeling Knowledge Domains}
\label{sec:background:ontology}

Ontologies provide a formal specification of concepts and relationships within a domain of knowledge. They enable shared understanding among humans and machines by defining classes, properties, and axioms that describe entities and their interrelations \cite{gruber1993ontology}. 

In the context of scientific experiments, ontologies can define experiment parameters, measurement units, procedures, and results. For example, an ontology might define a class \texttt{PropertyValue} representing any measured property, a class \texttt{Method} representing experimental procedures, and relationships such as \texttt{hasParameter} and \texttt{investigates} to link methods to parameters and observed metrics.

Ontologies serve as the backbone for semantic data integration and reasoning. They allow data from heterogeneous sources to be interpreted consistently, facilitate interoperability, and enable advanced queries across datasets using formal reasoning engines.

\section{Semantic Web Concepts}
\label{sec:background:semantic_web}

The Semantic Web extends the World Wide Web by enabling data to be represented in a machine-readable, semantically rich form. Data is described using standards such as RDF (Resource Description Framework), which encodes information as triples consisting of subject, predicate, and object. This allows heterogeneous data to be interlinked and queried using semantic technologies \cite{berners2001semantic}.

JSON-LD is one of the key technologies bridging conventional JSON data and the Semantic Web. By adding semantic annotations, JSON-LD enables JSON documents to be interpreted as RDF graphs. For instance, in the example simulation data (Listing \ref{lst:background_jsonld_example}), the simulation parameter `element-size` is represented as a `schema:PropertyValue` with a QUDT unit:

\begin{lstlisting}[language=json, caption={Example of semantic annotation of a parameter with QUDT unit}, label={lst:background_qudt_example}]
{
    "@id": "local:variable_element_size",
    "@type": "schema:PropertyValue",
    "rdfs:label": "element-size",
    "schema:unitCode": {
        "@id": "unit:M"
    },
    "schema:value": 0.025
}
\end{lstlisting}

Here, the unit is linked to the QUDT ontology (`http://qudt.org/vocab/unit/`) using its URI `unit:M`. This allows applications and knowledge graph systems to unambiguously interpret the measurement unit and integrate it with other semantically annotated datasets. Similarly, experiment methods and metrics are linked to well-defined ontology classes such as `m4i:Method` and `schema:PropertyValue`.

Semantic Web technologies thus provide a framework for:

\begin{itemize}
    \item Interoperable data representation across different systems and domains.
    \item Linking and reasoning over experimental data using ontologies.
    \item Querying and integrating datasets through standard languages like SPARQL.
\end{itemize}

By grounding JSON data in ontologies and RDF through JSON-LD, simulation results become part of the Semantic Web, enabling more advanced analysis, automated reasoning, and integration with other scientific knowledge graphs.

\section{Semantic Data Representation using JSON-LD}
\label{sec:background:jsonld}

JSON-LD (JavaScript Object Notation for Linked Data) extends JSON by introducing semantic annotations that allow data to be interpreted as linked data within the Resource Description Framework (RDF). JSON-LD integrates JSON with established semantic web standards by providing mechanisms for defining contexts, identifiers, and relationships between entities \cite{jsonld2014}.

In the context of scientific experiments, JSON-LD allows simulation parameters and results to be linked to well-defined ontologies and vocabularies. For instance, parameters can be modeled using \texttt{schema:PropertyValue}, while experiment procedures can be represented using domain ontologies such as metadata4ing.

Listing \ref{lst:background_jsonld_example} shows a JSON-LD representation corresponding to the example simulation result.

\begin{lstlisting}[language=json, caption={JSON-LD representation of the simulation result}, label={lst:background_jsonld_example}]
{
    "@context": {
        "local": "https://www.example.org/",
        "schema": "https://schema.org/",
        "m4i": "http://w3id.org/nfdi4ing/metadata4ing#",
        "unit": "http://qudt.org/vocab/unit/"
    },
    "@graph": [
        {
            "@id": "local:run_fenics_simulation",
            "@type": "m4i:Method",
            "m4i:hasParameter": [
                [
                    {
                        "@id": "local:variable_element_size"
                    }
                ]
            ],
            "m4i:investigates": [
                [
                    {
                        "@id": "local:variable_max_von_mises_stress"
                    }
                ]
            ]
        },
        {
            "@id": "local:variable_element_size",
            "@type": "schema:PropertyValue",
            "rdfs:label": "element-size",
            "schema:unitCode": {
                "@id": "unit:M"
            },
            "schema:value": 0.025
        },
        {
            "@id": "local:variable_max_von_mises_stress",
            "@type": "schema:PropertyValue",
            "rdfs:label": "max_von_mises_stress",
            "schema:value": 299791507.5586333
        }
    ]
}
\end{lstlisting}

This representation enriches the raw simulation output with semantic context, enabling interoperability with knowledge graphs and semantic data infrastructures.

\section{RML for Mapping JSON to RDF}
\label{sec:background:rml}

The RDF Mapping Language (RML) is a declarative mapping language used to transform heterogeneous data sources into RDF representations. RML extends the W3C-standardized R2RML language and enables mappings from various data formats, including JSON, XML, CSV, and relational databases, into RDF datasets \cite{dimou2014rml}. 

RML mappings are themselves expressed as RDF graphs and specify how elements from a data source should be transformed into RDF triples. The mapping process defines logical sources, subject maps, and predicate-object maps that together generate RDF statements.

Listing \ref{lst:background_rml_mapping} shows an example RML mapping that transforms the JSON document in Listing \ref{lst:background_json_example} into the JSON-LD structure presented in Listing \ref{lst:background_jsonld_example}.

\begin{lstlisting}[language=turtle, caption={RML mapping from JSON simulation output to RDF/JSON-LD}, label={lst:background_rml_mapping}]
@prefix rml: <http://semweb.mmlab.be/ns/rml#>.
@prefix rr: <http://www.w3.org/ns/r2rml#>.
@prefix ql: <http://semweb.mmlab.be/ns/ql#>.
@prefix schema: <https://schema.org/>.
@prefix m4i: <http://w3id.org/nfdi4ing/metadata4ing#>.
@prefix unit: <http://qudt.org/vocab/unit/>.
@prefix local: <https://www.example.org/>.

<#MethodMapping>
    rml:logicalSource [
        rml:source "simulation.json" ;
        rml:referenceFormulation ql:JSONPath ;
        rml:iterator "$"
    ];
    rr:subjectMap [
        rr:constant local:run_fenics_simulation ;
        rr:class m4i:Method ;
    ];
    rr:predicateObjectMap [
        rr:predicate m4i:hasParameter ;
        rr:objectMap [
            rr:constant local:variable_element_size
        ]
    ];
    rr:predicateObjectMap [
        rr:predicate m4i:investigates ;
        rr:objectMap [
            rr:constant local:variable_max_von_mises_stress
        ]
    ].

<#ElementSizeMapping>
    rml:logicalSource [
        rml:source "simulation.json" ;
        rml:referenceFormulation ql:JSONPath ;
        rml:iterator "$.parameters.element-size"
    ];
    rr:subjectMap [
        rr:constant local:variable_element_size ;
        rr:class schema:PropertyValue ;
    ];
    rr:predicateObjectMap [
        rr:predicate schema:value ;
        rr:objectMap [
            rml:reference "value"
        ]
    ];
    rr:predicateObjectMap [
        rr:predicate schema:unitCode ;
        rr:objectMap [
            rr:constant unit:M
        ]
    ].

<#StressMetricMapping>
    rml:logicalSource [
        rml:source "simulation.json" ;
        rml:referenceFormulation ql:JSONPath ;
        rml:iterator "$.metrics"
    ];
    rr:subjectMap [
        rr:constant local:variable_max_von_mises_stress ;
        rr:class schema:PropertyValue ;
    ];
    rr:predicateObjectMap [
        rr:predicate schema:value ;
        rr:objectMap [
            rml:reference "max_von_mises_stress_nodes"
        ]
    ].
\end{lstlisting}

Through this mapping process, structured simulation outputs can be automatically converted into semantically enriched knowledge graph representations, enabling integration with semantic data infrastructures and facilitating interoperability across scientific systems.